The dataset is highly imbalanced (~88% not popular), which inflates accuracy. This highlights the limitation of using accuracy alone as a metric.

After handling class imbalance, the model achieved a recall of 0.63 for popular songs, meaning it correctly identifies a majority of them. However, a significant portion is still missed, indicating that audio features alone are not sufficient to fully predict popularity.
